---
name: AI platform
description: |
  Discover, compare, and run AI models using AI platform's API. Strong fit for image, audio, and video generation pipelines that swap models frequently.
triggers:
  - "AI platform"
  - "run ai model"
  - "model comparison"
  - "AI platform api"
od:
  mode: image
  category: image-generation
---

# AI platform

> Curated from AI platform.

## What it does

Discover, compare, and run AI models using AI platform's API. Strong fit for image, audio, and video generation pipelines that swap models frequently.

## Source

- Category: `image-generation`

## How to use

This catalogue entry advertises the skill in Frontend Design so the agent
discovers it during planning. To run the full upstream workflow with
its original assets, scripts, and references, install the upstream
bundle into your active agent's skills directory:

```bash
# Inspect the upstream README for exact paths
```

Then ask the agent to invoke this skill by name (`AI platform`) or with
one of the trigger phrases listed in this skill's frontmatter.
