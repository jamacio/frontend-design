> Read `.github/AGENTS.md` for the full design super agent context.

This project uses `.github/` as its design knowledge base. Before any design work, read `.github/AGENTS.md`. All craft rules, design systems, brand references, and templates are in `.github/`.
